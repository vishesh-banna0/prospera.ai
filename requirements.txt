# Core backend framework
fastapi==0.116.1
uvicorn[standard]==0.35.0

# Configuration and validation
pydantic==2.11.7
pydantic-settings==2.10.1
python-dotenv==1.1.1

# Database and persistence
sqlalchemy==2.0.41
alembic==1.16.2
psycopg[binary]==3.2.13
redis==6.2.0

# HTTP clients for external market data integrations
httpx==0.28.1
yfinance==0.2.65

# Multi-agent orchestration for the AI Advisor (LangGraph agent graph). LLM
# calls still go through the app's own OpenAI-compatible client, so this is used
# for orchestration only.
langgraph==1.2.9

# Vector search for the research knowledge base (RAG). FAISS holds an exact
# inner-product index over L2-normalized embeddings, so its scores are cosine
# similarities and it ranks identically to the SQL fallback — set
# RESEARCH_VECTOR_BACKEND=sql to drop back to the pure-Python scan.
faiss-cpu==1.14.3

# Numerical kernel for FAISS and for portfolio optimization / risk analytics
# (covariance, mean-variance weights, risk contributions).
numpy==2.4.6

# Utilities that are commonly needed in backend services
python-dateutil==2.9.0.post0
tenacity==9.1.2
